# 垂直轴风轮载荷数据处理系统 - Python依赖清单
# 安装方式：pip install -r requirements.txt
# Python 版本要求：3.9 及以上（推荐 3.10 - 3.12）

# ===== 核心科学计算 =====
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
matplotlib>=3.4.0

# ===== 文件导出 =====
# Excel 导出
openpyxl>=3.0.0
# PDF 报告导出
reportlab>=4.0.0
# Word 报告导出（python-docx）
python-docx>=1.0.0
# 图片处理（Word报告插图依赖）
pillow>=9.0.0

# ===== 可选：高性能列存 / 内存监控 =====
# 中间结果保存（如不保存可注释）
pyarrow>=6.0.0
# 内存监控（可选）
psutil>=5.8.0

# 工况清单 (case.yaml) 可选依赖；不安装可用 cases.csv / cases.txt 替代
pyyaml>=6.0.0
